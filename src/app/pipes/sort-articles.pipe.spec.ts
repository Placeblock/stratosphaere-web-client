import { SortArticlesPipe } from './sort-articles.pipe';

describe('SortArticlesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortArticlesPipe();
    expect(pipe).toBeTruthy();
  });
});
